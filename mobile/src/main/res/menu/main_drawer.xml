<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_home"
            android:title="Startseite" />

    </group>

    <item android:title="Zeiten-Management">
        <menu>
            <item
                android:id="@+id/nav_timereg_show"
                android:icon="@drawable/ic_list"
                android:title="Übersicht" />
            <item
                android:id="@+id/nav_timereg_add"
                android:icon="@drawable/ic_add"
                android:title="Hinzufügen" />
        </menu>
    </item>

    <item android:title="Projekte">
        <menu>
            <item
                android:id="@+id/nav_projects_show"
                android:icon="@drawable/ic_list"
                android:title="Übersicht" />
            <item
                android:id="@+id/nav_projects_add"
                android:icon="@drawable/ic_add"
                android:title="Hinzufügen" />
        </menu>
    </item>

    <item android:title="Einstellungen">
        <menu>
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_settings"
                android:title="Einstellungen" />
        </menu>
    </item>

</menu>
